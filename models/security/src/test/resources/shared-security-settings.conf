#  @(#) Shared test settings for models-security

foundation {
	deployment = "urn:deployment:unit-test"

	security {
		error-timeout = 10s
		loading-timeout = 2s

		retries = 2
		time-range = 1s

		passwords = "vault:/unit-test/passwords"
	}
}

